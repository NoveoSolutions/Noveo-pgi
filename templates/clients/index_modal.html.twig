</div class="card">               
    {{form_start (form)}}
        <button class="btn btn-primary" type="submit">Enregistrer</button>
    {{form_end (form)}}
    {% if content|default %}
    {{ content|default|raw }}
    {% endif %}
</div>